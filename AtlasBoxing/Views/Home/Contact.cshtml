<main aria-labelledby="title">
    <h2 id="title" style="text-align: center;">@ViewBag.Title</h2>
    <br />
    <h3 style="text-align: center;">@ViewBag.Message</h3>
    <br />

    <!-- Wrapper for both the address and form containers -->
    <div class="address-form-container">
        <!-- Address Section -->
        <div class="address-container">
            <address>
                <b>Address</b>
                <br />
                65a Chesterfield Road
                <br />
                Leicester
                LE5 5LH
                <br />
                <b>Contact Us</b>
                <br />
                <b>Phone: 0739992263</b>
                <br />
                <strong>Email:</strong>
                <a href="mailto:atlasboxing@yahoo.com">atlasboxing@yahoo.com</a>
                <br />
            </address>
        </div>

        <!-- Contact Form with Centering and Box -->
        <div class="form-container">
            <h3>Send Us a Message</h3>
            <form method="post" action="@Url.Action("SendMessage", "Home")">
                <div class="form-group">
                    <label for="name">Your Name:</label>
                    <input type="text" id="name" name="Name" required class="form-input" />
                </div>
                <br />
                <div class="form-group">
                    <label for="email">Your Email:</label>
                    <input type="email" id="email" name="Email" required class="form-input" />
                </div>
                <br />
                <div class="form-group">
                    <label for="message">Your Message:</label>
                    <textarea id="message" name="Message" rows="5" required class="form-input"></textarea>
                </div>
                <br />
                <button type="submit">Send Message</button>
            </form>
        </div>
    </div>
</main>

<style>
    /* Main wrapper for centering the content */
    .address-form-container {
        display: flex;
        justify-content: center; /* Centers horizontally */
        align-items: flex-start; /* Aligns containers at the top */
        min-height: 100vh;
        padding: 20px;
        box-sizing: border-box;
    }

    /* Address and Form Container Styles */
    .address-container,
    .form-container {
        width: 40%; /* Adjust as needed */
        margin: 0 20px; /* Adds space between the two containers */
    }

    /* Additional margin to push the address container higher */
    .address-container {
        margin-top: -0px; /* Adjust this value to control how much higher the address container appears */
    }

    /* Form styling */
    .form-container {
        padding: 20px;
        border: 2px solid #ccc;
        border-radius: 10px;
        background-color: #f9f9f9;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .form-group {
        margin-bottom: 15px;
    }

        .form-group label {
            display: block;
            margin-bottom: 5px;
        }

    .form-input {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
    }

    button[type="submit"] {
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
    }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }
</style>
